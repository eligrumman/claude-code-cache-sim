// tape.js - canvas Tape renderer (blue read / red write lanes, TTL drain bars).
// TO PORT from build/tape-prototype.html draw()/tick()/simulateSend visuals.
// Consumes engine ledger rows; owns no economics.
export function drawTape() { /* stub */ }
