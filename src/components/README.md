# src/components - compact control components
Dense ncase-style Svelte controls for the config strip (small-selects, sliders,
toggles, checkboxes) per SIMULATOR_SPEC.md Section 8. To build next phase.
