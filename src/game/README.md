# src/game - the reducer over the SDLC unit graph
The pure step(state, action) reducer (SIMULATOR_SPEC.md Section 5.3), unit graph
(3.3), rework/PRNG model (3.4/5.5), win/lose (6), report (7). Uses immer for the
GameState transitions. To build next phase.
